pkgname=test-split-as-makedep-client
pkgver=1.0
pkgrel=1
pkgdesc='aurox test fixture: client makedepending on a split pkg name'
arch=('any')
license=('MIT')
makedepends=('test-split-as-makedep-helper-lib')

build() {
    test -f /usr/share/test-split-as-makedep-helper-lib/marker
}

package() {
    install -Dm644 /dev/null "$pkgdir/usr/share/$pkgname/marker"
}
